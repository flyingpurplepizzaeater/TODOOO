---
phase: 05-todo-integration
plan: 04
type: execute
wave: 3
depends_on: ["05-01", "05-02", "05-03"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "TODO cards render correctly with all visual elements"
    - "Checkbox toggles completed state"
    - "Completed cards show green tint + checkmark + strikethrough"
    - "Overdue cards show red styling"
    - "Frame presets create correct layouts"
    - "TODOs dropped in frames become children"
---

<objective>
Verify complete TODO integration functionality through manual testing.

Purpose: Ensure all Phase 5 requirements (TODO-01 through TODO-04) work correctly in the browser. This includes visual verification of card styling, interaction testing, and frame grouping behavior.

Output: Verification that all TODO features work as specified, documented issues if any found.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-todo-integration/05-CONTEXT.md
@.planning/phases/05-todo-integration/05-01-SUMMARY.md
@.planning/phases/05-todo-integration/05-02-SUMMARY.md
@.planning/phases/05-todo-integration/05-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start development servers and prepare test environment</name>
  <files></files>
  <action>
1. Start backend server:
   ```bash
   cd "C:\Users\Workshop\Desktop\AI-2\Claude RW+skills\TODO"
   uvicorn main:app --reload --port 8000
   ```

2. Start frontend dev server (in separate terminal):
   ```bash
   cd "C:\Users\Workshop\Desktop\AI-2\Claude RW+skills\TODO\frontend"
   npm run dev
   ```

3. Verify both servers running:
   - Backend: http://localhost:8000/docs shows FastAPI docs
   - Frontend: http://localhost:5173 (or assigned port) shows canvas

4. Ensure test credentials available:
   - If no user exists, register one via POST /auth/register
   - Get JWT token via POST /auth/token
   - Create or use existing team and list for TODO sync testing
  </action>
  <verify>
- Backend responds at localhost:8000
- Frontend responds at localhost:5173 (or similar)
- Canvas component renders
  </verify>
  <done>
Development environment running and ready for testing.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 5 TODO Integration features:
1. Custom TODO shape with visual styling
2. Bidirectional backend sync
3. Frame presets for task grouping
  </what-built>
  <how-to-verify>
**Test TODO Card Creation (TODO-01):**
1. Open canvas in browser (http://localhost:5173)
2. Click TODO tool button in toolbar OR press 'T' key
3. Click on canvas to create a TODO card
4. Verify card appears with:
   - Title "New Task"
   - Checkbox on left
   - Medium priority (yellow left border)

**Test Status Visualization (TODO-02):**
1. Click the checkbox on a TODO card
2. Verify completed state shows:
   - Green background tint (#dcfce7)
   - Checkmark in checkbox
   - Title has strikethrough

3. Create a TODO and manually set an overdue date (if editable):
4. Verify overdue shows:
   - Red date text
   - Red border on card

**Test Frame Grouping (TODO-03):**
1. Click "Frames" dropdown in toolbar
2. Select "Kanban Board"
3. Verify 3 frames created: "To Do", "In Progress", "Done"
4. Create a TODO card
5. Drag the TODO into the "To Do" frame
6. Verify the TODO becomes a child of the frame (moves with frame)

7. Select "Eisenhower Matrix" from Frames
8. Verify 4 quadrant frames created with proper labels

**Test Backend Sync (TODO-04):**
(Only if defaultListId is configured)
1. Open browser dev tools > Network tab
2. Create a TODO card
3. Verify POST request to /lists/{id}/todos
4. Click checkbox to complete
5. Verify PUT request to /todos/{id}
6. Delete the TODO card
7. Verify DELETE request to /todos/{id}

**Test TODO Tool Integration:**
1. Press '1' key - should switch to select tool
2. Press 'T' key - should switch to TODO tool
3. Click canvas - should create TODO card
4. Press '1' again - should switch back to select

**Report any issues found:**
- Visual styling incorrect
- Interactions not working
- Sync not functioning
- Errors in browser console
  </how-to-verify>
  <resume-signal>
Type "approved" if all tests pass, or describe any issues found that need fixing.
  </resume-signal>
</task>

</tasks>

<verification>
All manual testing completed through checkpoint task.
</verification>

<success_criteria>
1. TODO cards can be created via toolbar and keyboard shortcut
2. Cards display title, checkbox, and priority border
3. Completed state shows green tint + strikethrough
4. Checkbox toggles completion state
5. Frame presets create correct layouts
6. TODOs dragged into frames become children
7. Backend sync works (if configured) with proper HTTP requests
8. No JavaScript errors in browser console
</success_criteria>

<output>
After completion, create `.planning/phases/05-todo-integration/05-04-SUMMARY.md`
</output>
