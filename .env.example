# Collaborative TODO App - Environment Configuration
# Copy this file to .env and fill in your values

# REQUIRED: Secret key for JWT token signing
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-secret-key-here

# Optional: Database URL (defaults to SQLite in project directory)
# DATABASE_URL=sqlite+aiosqlite:///./todo.db
# DATABASE_URL=postgresql+asyncpg://user:pass@localhost/todoapp

# Optional: JWT token expiration in hours (default: 24)
# ACCESS_TOKEN_EXPIRE_HOURS=24
